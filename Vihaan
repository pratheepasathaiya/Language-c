#include <stdio.h>

int main() {
    int n, i, num;
    int sum = 0;
    scanf("%d", &n);

    
    for(i = 0; i < n; i++) {
        scanf("%d", &num);
        if(num > 0) {
            sum += num * num;
        }
    }

    // Output the result
    printf("%d\n", sum);

    return 0;
}
